#here we have all linux commands 
 go to command promt 
 pwd ->current working directory 
 cd ->change directory 
 ls ->list of folders
 ls -a ->list of hif=deen directory (these start with .filenames)
 clear 
 
